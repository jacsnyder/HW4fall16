-# in app/views/users/show.html.haml

%h2 Details about #{@user.user_id}

%ul#details
  %li
    User-ID:
    = @user.user_id
  %li
    E-mail:
    = @user.email

= link_to 'Edit', edit_user_path(@user)
= button_to 'Delete', user_path(@user), :method => :delete, :confirm => 'Are you sure?'
= link_to 'Back to user list', users_path
